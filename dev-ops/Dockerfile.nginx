# QA Studio Nginx - 反向代理 + 静态文件服务
FROM nginx:alpine

# Copy nginx configuration
COPY dev-ops/nginx/default.conf /etc/nginx/conf.d/default.conf

# 静态文件通过 volume 从 frontend 容器挂载
# /usr/share/nginx/html 由 docker-compose 挂载

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]