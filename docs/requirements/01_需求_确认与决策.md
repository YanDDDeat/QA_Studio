# QA_Studio 需求确认

以下是对需求文档中不明确或需要决策的细节，请逐条回复。

## 1. 数据库选择

目前需求中标记为"？？"，建议方案：

- **PostgreSQL**：支持 JSONB，适合存大模型返回的复杂结构，成熟稳定，适合生产环境
- **SQLite**：轻量零配置，适合开发/小规模使用，并发能力弱

你的选择或偏好？ 总管数据可能需要生成几十万条问答，加上上传的text数据，你建议什么？

## 2. Pipeline 数据流转方式

需求提到"不用每次下载再上传"，请确认：

- **方案A**：每个阶段是独立的任务，用户在前端选择"使用上一阶段的输出"作为输入，可以灵活跳步或重复某个阶段
- **方案B**：整个 Pipeline 按顺序串联，用户一键启动，从问题生成到数据评估自动依次执行

你倾向哪种？方案A，按我给的层级进行前端页面建立，用户自己操作，然后每次输出的JSON用户可以选择写完后覆盖，或者是产生新的文件。

## 3. LLM 调用

### 3.1 支持的模型接口

- 只需要支持 OpenAI 兼容接口（一个 baseURL + API Key 即可接入大部分模型）？
- 还是需要分别对接多家（如 Anthropic、国产模型各自独立配置）？
LLM 调用我会给你一个接口文档，反正你抽离出来，对外提供一个方法即可。输入提示词，返回LLM的文本解析即可
aliyun:
key:sk-1744b0734d374ec7af64f5d87d8ba3e1
curl -X POST https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions \
-H "Authorization: Bearer sk-1744b0734d374ec7af64f5d87d8ba3e1" \
-H "Content-Type: application/json" \
-d '{
    "model": "qwen3-max",
    "messages": [
        {
            "role": "system",
            "content": "You are a helpful assistant."
        },
        {
            "role": "user",
            "content": "你是谁？"
        }
    ]
}'
### 3.2 并发与进度

- 一个 JSON 有 100 条 text，逐条调用还是支持批量并发？
    逐条调用吧，因为可能有多个用户同时用，若批量并发是否会有影响？
- 用户是否需要实时看到每条的生成进度（比如已完成 23/100）？
    对，你给一个进度条，并且显示当前已经生成了的量。因为一个JSON里面有多个text文本段，然后一个text文本可能会产生多个问答对。

## 4. 各阶段字段归属

根据流程，推测各阶段生成的字段如下，请确认是否正确：

| 阶段 | 生成的字段 |
|------|-----------|
| 问题生成 | input, category, task_type, domain |
| 知识体系生成 | knowledge |
| 问题校验 | passed（初筛） |
| 答案生成 | output, cot |
| 答案校验 | passed（终筛） |
| 数据评估 | difficulty, relevance, clarity, reasoning, terminology, score |
| 固定/来源字段 | originContent（来自输入 text），source, source_id, source_type, corpus_cate（固定1），Assessment（空） |

如有字段归属不对，请指出。
| 阶段 | 解释 | 生成的字段
|------|-----------|-----------|
| 问题生成 | 在本阶段中，category在用户点击开始生成前，由用户选择确定枚举["知识问答","逻辑生成"]；task_type、input、domain由大模型生成后的解析获得；corpus_cate固定为1；id生成一个全局唯一ID，整个系统级别的唯一ID。source，source_id 这两个字段，若用户填写，则直接使用。若为空，则对应的JSON里面{}数据有source字段，则直接赋值，若没有则使用上传的JSON文件名;source_type用户可自己选择其中之一[图书、专利、文献、其他] |id，input, category, task_type, domain，difficulty，corpus_cate，source，source_id,source_type |
| 知识体系生成| 只会返回konwledge，但是scene字段你手动赋值为与knowledge一样 | knowledge |
| 问题校验 | 根据返回的validation_result来判断通过（=="PASS"）与否，不用保留这些字段，若不通过写入到新的JSON文件并保留源字段+这几个字段 |  { "validation_result": "PASS", "reason": "通过校验或失败原因" }|
| 答案生成 | 生成output、cot |output, cot |
| 答案校验 | 根据返回的validation_result来判断通过（=="PASS"）与否，不用保留这些字段，若不通过写入到未通过的JSON文件保留源字段+这几个字段 |  { "validation_result": "PASS", "reason": "通过校验或失败原因" }|
| 数据评估 | LLM生成 | relevance, clarity, reasoning, terminology, score |
| 固定/来源字段 | originContent（来自输入 text），source, source_id, source_type, corpus_cate（固定1），Assessment（空） |



## 5. 校验阶段的 PASS 逻辑

校验质量时，LLM 返回 `passed: "否"` 的记录：

- **方案A**：直接丢弃，不进入下一阶段
- **方案B**：标记为不通过但保留在数据里，用户仍可查看，只是不参与后续流程

你的选择？
passed固定为通过。

## 6. scene 字段

数据格式中有 `scene` 字段（如"过程仿真与验证"），这个字段：

- 由用户在上传时手动指定？
- 由某个阶段的 LLM 生成？
- 其他方式？
scene同konwledge字段
## 7. source / source_id / source_type

这几个字段看起来与输入文件有关：

- `source`（如"单质与混合火工药剂"）：从哪里获取？用户指定还是从文件名/路径推断？
- `source_id`（如"单质与混合火工药剂__src_2f6f6072__ch_6bf9d5f8__sec_b3f5c2a5__chunk_028"）：是输入 JSON 中自带还是系统自动生成？
- `source_type`（如"图书"）：用户指定还是从文件元数据获取？
在 ## 4 各阶段字段归属已解答
## 8. id 字段

`id` 标注为"唯一ID"，是系统自动生成的 UUID，还是需要特定格式？
弄一个全局数字。
## 9. 多用户与权限

需求提到"简单的用户登录与隔离"，请确认：

- 是否需要注册功能，还是管理员手动创建账号？ 手动创建
- 是否需要角色区分（如管理员 vs 普通用户）？管理员能否查看所有人的数据？ 暂时不用区分
- 还是只要最简单的：每个用户只能看到自己的数据，没有角色区分？ 最简单即可

---

## 10. 追加确认

### 10.1 difficulty 字段来源

`difficulty` 同时出现在问题生成和问题校验阶段，最终记录中保留哪个阶段的值？

- **方案A**：校验阶段的 difficulty 覆盖生成阶段的
- **方案B**：保留问题生成阶段的，校验阶段只产出 validation_result 和 reason
已经修改了，只有生成问题阶段需要difficulty
### 10.2 校验失败的数据文件

问题/答案校验失败的数据"写入到新的JSON文件并保留源字段+校验字段"——这个文件：

- 系统自动生成，用户在工作区文件管理中查看/下载？
- 还是其他处理方式？
系统自动生成，用户在工作区文件管理中查看/下载
### 10.3 覆盖 vs 新文件的交互时机

用户选择"覆盖原文件"还是"产生新文件"——这个选项：

- **方案A**：每个阶段完成后弹出选项让用户决定
- **方案B**：在开始任务前由用户选择默认行为
每个阶段的输出写回到同一个文件，在原记录上追加字段，不需要用户选择。
### 10.4 domain 字段约束

问题生成阶段 domain 由 LLM 生成——值是否需要约束（如用户预设的枚举列表），还是完全由 LLM 自由生成？
domain由LLM生成。
